# Python dependencies for detect_divergent_transcription.py
# Used when running without conda/docker. The pipeline prefers:
#   -profile conda  (uses envs/tracktx.yaml)
#   -profile docker (pre-built image with all deps)
#
# Manual install: pip install -r envs/requirements-divergent.txt
numpy>=1.26
pandas>=2.2
scikit-learn>=1.3
scipy>=1.11
